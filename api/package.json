{
  "name": "feast-api",
  "version": "1.0.0",
  "main": "dist/application.js",
  "license": "MIT",
  "scripts": {
    "start": "tsc -w --outDir dist/ | nodemon --watch dist/",
    "clean": "rimraf dist/",
    "compile": "tsc --noEmit -p src/"
  },
  "devDependencies": {
    "@types/cors": "^2.8.7",
    "@types/express": "^4.17.8",
    "rimraf": "^3.0.2",
    "typescript": "^4.0.3"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1"
  }
}
