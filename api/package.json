{
  "name": "feast-api",
  "version": "1.0.0",
  "main": "dist/api/src/application.js",
  "license": "MIT",
  "scripts": {
    "start": "tsc -w --outDir dist/ | nodemon --watch dist/",
    "clean": "rimraf dist/",
    "compile": "tsc --noEmit -p src/"
  },
  "devDependencies": {
    "@types/cors": "^2.8.7",
    "@types/express": "^4.17.8",
    "@types/shortid": "^0.0.29",
    "feast-domain": "file:../domain",
    "nodemon": "^2.0.4",
    "rimraf": "^3.0.2",
    "typescript": "4.0.3"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "purify-ts": "^0.16.0-beta.5",
    "shortid": "^2.2.15"
  }
}
